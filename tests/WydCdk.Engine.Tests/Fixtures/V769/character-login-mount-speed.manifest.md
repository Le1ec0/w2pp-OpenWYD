# Character-login mount run-floor resolution (7.69)

- Target: `Backup/Tools/ReferenceSources/TMProject2GlobalClient/Servidor/Source/Code/Basedef.cpp`, `g_pMountBonus[30][5]` at lines 238-273, `g_pMountTempBonus[20][5]` at 275-296, and the final mount gate at 4450-4469. Source SHA-256: `D84B2E358825CC0AF1FF8D0CFD316E09304892C15422A48C3D94A3D047927610`.
- Rule: standard item indices 2360..2389 use the fifth table column only when `stEffect[0].sValue > 0`; temporary indices 3980..3994 use the fifth temporary-table column without that effect gate. The cached `face <= 4` condition remains in `ApplyFinalAttackRunStage` and is applied after this resolution.
- Target table delta: standard mounts 2360..2365 have run floors `[4,4,5,5,4,5]`; remaining in-bounds standard entries and all used temporary entries have floor 6.
- W2PP comparison: `Server/W2PP/Source/Code/Basedef.cpp` declares the same table dimensions and mount gate, but its fifth standard-mount column is 6 for all 30 entries (the temporary entries used here are also 6). C# follows the target 7.69 data, not the changed W2PP standard-mount values. W2PP source SHA-256: `C9E04E7F0AAC87DFDAB367B77F3DC8384355FE1166898FFEB62E095C551B99F7`.
- Bounds defect: both source gates include standard item index 2390, but `g_pMountBonus` has only 30 rows (valid indices 2360..2389). With a positive first effect value, the source indexes row 30 out of bounds; runtime behavior is undefined. `LegacyMountRunRules` safely rejects that item rather than reproducing the invalid read. This case deliberately has no W2PP expected value.
- Port: `LegacyMountRunRules.TryResolveRunFloor` resolves the table/effect portion only; it does not test cached face or mutate `MOB.CurrentScore`. The final AttackRun stage applies the face gate and clamps/packing separately.
- Fixture: all 30 standard and all 15 source-reachable temporary table values are covered, plus range/effect boundaries. These are source-derived checks, not a compiled C++ runtime capture.
