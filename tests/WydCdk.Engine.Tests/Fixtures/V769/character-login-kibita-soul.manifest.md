# Character-login Kibita Soul (7.69)

- Target: `Backup/Tools/ReferenceSources/TMProject2GlobalClient/Servidor/Source/Code/Basedef.cpp::BASE_GetCurrentScore`; detection at lines 3297-3298 and late override at lines 4396-4408. SHA-256: `D84B2E358825CC0AF1FF8D0CFD316E09304892C15422A48C3D94A3D047927610`.
- Detection: only when not a summon, `ClassMaster == MORTAL`, `CurrentScore.Level < 369`, and at least one of the 32 affect records has `Type == 29`. The level boundary is exclusive: level 368 selects Kibita; level 369 follows ordinary Mortal Soul math.
- Late target mutation: `Str=2000`, `Int=2000`, `MaxHp=10000`, then `ItemInt=(CurrentScore.Int - BaseScore.Int > 2000 ? 2000 : BaseScore.Int)` using the already-overridden Int, followed by `MaxMp += ItemInt * 2`. The early Soul HP/MP stage must not also apply ordinary Mortal Soul scaling for the Kibita branch.
- W2PP comparison: `Server/W2PP/Source/Code/Basedef.cpp::BASE_GetCurrentScore`; detection at lines 3099-3100 and late override at lines 4635-4648. SHA-256: `C9E04E7F0AAC87DFDAB367B77F3DC8384355FE1166898FFEB62E095C551B99F7`. The gate and Str/Int/MaxHp assignments match, as does the `ItemInt` expression. MaxMP does not: W2PP computes `mmp = MaxMp + MPDelta` then adds `mmp`, while 7.69 has that temporary commented out and adds only `MPDelta`. C# follows 7.69; for initial MaxMP 500 and ItemInt 100 this yields 700, while W2PP yields 1200.
- Port: `ApplySoulHealthManaStage` now bypasses ordinary Mortal Soul scaling below level 369; `ApplyKibitaSoulStage` separately models the later mutation, preserving operation order and input immutability. Both remain isolated from the login coordinator/listener.
- Fixture: `character-login-kibita-soul.golden.json`; seven synthetic vectors cover levels 368/369, missing/repeated affects, summon bypass, both `ItemInt` branches, and the target/W2PP MaxMP divergence. These are source-derived arithmetic cases, not compiled C++ runtime captures or player data.
- The ordinary Soul fixture now explicitly uses level 400 for its Mortal vectors. The known 7.69 common/TMSrv ABI mismatch remains; no runtime parity claim is made.
