# Character-login transformation damage stage (7.69)

- Target: `Backup/Tools/ReferenceSources/TMProject2GlobalClient/Servidor/Source/Code/Basedef.cpp::BASE_GetCurrentScore`, `Affect.Type == 16` at lines 3711-3792; the selected common file is included by `TMSrv.vcxproj` as `..\Basedef.cpp` (line 101). Source SHA-256: `D84B2E358825CC0AF1FF8D0CFD316E09304892C15422A48C3D94A3D047927610`.
- Formula: for each valid value 1..5 and class 2, calculate `multi = (delta * Affect.Level) / 200 + min`, where `min/max = DamAdd + pTransBonus[value-1].Unk/Unk2`; accumulate `multi - 100` into the existing `DAMAGEMULTI`. Every Wolf transform also adds 10 directly to `CurrentScore.Damage`, regardless of the Wolf-skill gate. The final score code applies the accumulated multiplier after the affect loop, so this stage returns the damage addition separately from the additive multiplier adjustment. `Affect.Level` is an unsigned 16-bit field; no clamp is present in this branch.
- Target `DamAdd`: Wolf 25 only with skill `0x20000`; Astaroth 10 only with `0x200000`; Eden 20 unconditionally; Bear/Titan 0. Target and W2PP share the five base `Unk/Unk2` rows. W2PP `DamAdd` differs for skilled Wolf (10, not 25) and Eden (10, not 20); Astaroth agrees. Both add the separate +10 Damage for each valid Wolf affect.
- W2PP: `Server/W2PP/Source/Code/Basedef.cpp::BASE_GetCurrentScore`, source SHA-256 `C9E04E7F0AAC87DFDAB367B77F3DC8384355FE1166898FFEB62E095C551B99F7`; the function aliases `Level = Affect[i].Level` before the affect dispatch and performs the same integer expression/order.
- Port: `LegacyCurrentScoreMath.CalculateTransformationDamageAffectStage` returns the sum of direct Damage additions and the multiplier delta. It does not mutate `MOB`, apply the overall multiplier, include non-Type=16 affects, or integrate with the final damage/login pipeline.
- Fixture: 16 synthetic vectors cover every form, skill gates, affect/class/value gates, integer truncation, repeated Wolf accumulation, and `ushort` level arithmetic. The test-only W2PP evaluator is checked against explicit comparator expectations; these are source-derived vectors, not compiled C++ runtime captures.
- Scope: Type=16 damage contribution only. Transformation AC, MaxHP, regeneration, critical/resistance/equipment mutation, integration, and client E2E remain separate/unfinished stages.
