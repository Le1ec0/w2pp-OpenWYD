# Character-login final physical damage stage (7.69)

- Target: `Backup/Tools/ReferenceSources/TMProject2GlobalClient/Servidor/Source/Code/Basedef.cpp::BASE_GetCurrentScore`, cached `face` at line 3423 and final damage block at lines 4422-4426. `MAX_LEVEL=399` in `Basedef.h` (target header SHA-256 `B59A7C5FC76D898C10E3600005920A56450725480C8C5D485DE8F199762307E8`). Source SHA-256: `D84B2E358825CC0AF1FF8D0CFD316E09304892C15422A48C3D94A3D047927610`.
- Formula: when the cached `face < 4`, add `Str/2 + Dex/3 + Special[0] + levelBonus`; `levelBonus` is the current level for ARCH/MORTAL and `level + MAX_LEVEL` otherwise. After that, if `DAMAGEMULTI != 100`, multiply damage by it and divide by 100 using integer arithmetic. The cached face is computed before the buff/transform loops; it is not the later head-item index.
- W2PP comparison: `Server/W2PP/Source/Code/Basedef.cpp::BASE_GetCurrentScore`, final block at lines 4651-4655, function SHA-256 `C9E04E7F0AAC87DFDAB367B77F3DC8384355FE1166898FFEB62E095C551B99F7`; `MAX_LEVEL=399` and ARCH/MORTAL branch match exactly for identical intermediate inputs. W2PP also has earlier class-specific damage additions (around lines 3259-3819) that are absent from the selected 7.69 target. Those earlier additions are not included or endorsed by this final-stage port, so this evidence does not establish parity of total damage.
- Port: `LegacyCurrentScoreMath.ApplyFinalDamageStage` receives the already accumulated damage and `DAMAGEMULTI`, plus the cached face, stats, special, level, and class master. It applies only the final target block and returns a new value; it does not mutate a MOB or compute upstream class/buff/mount multipliers.
- Fixture: `character-login-final-damage.golden.json` contains eight synthetic vectors for the face gate, mortal/arch/other class-master level selection, integer division order, negative stat truncation, and multiplier application. The test-only W2PP formula is independently compared for each same-input vector. These are source-derived arithmetic tests, not compiled C++ runtime captures or full-score parity tests.
- Scope: isolated final damage arithmetic only. Earlier 7.69 affects, transformation effects, mount/passive modifiers, `DAMAGEMULTI` construction, full C# score orchestration, listener integration, and client E2E remain separate work.
