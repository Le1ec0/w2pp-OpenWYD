# Character-login transformation armor-class stage (7.69)

- Target: `Backup/Tools/ReferenceSources/TMProject2GlobalClient/Servidor/Source/Code/Basedef.cpp::BASE_GetCurrentScore`, `Affect.Type == 16` at lines 3711-3820; `TMSrv.vcxproj` includes the common `..\\Basedef.cpp` at project line 101. Source SHA-256: `D84B2E358825CC0AF1FF8D0CFD316E09304892C15422A48C3D94A3D047927610`.
- Formula per valid value 1..5 and class 2: `min = AcAdd + pTransBonus[value-1].Unk3`, `max = AcAdd + Unk4`, `multi = (max-min)*Affect.Level/200 + min`; then mutate the current AC as `AC = AC*multi/100` using source integer truncation. Wolf additionally adds 5 after that calculation on every valid Wolf affect, even without its skill. Subsequent affects operate on this already-mutated AC, so slot order matters.
- Target `AcAdd`: Wolf 0, Bear 0, Astaroth 5 only with skill `0x200000`, Titan 20, Eden 10. The five base Min/Max AC rows are `[95,105]`, `[100,110]`, `[105,115]`, `[110,125]`, `[110,120]`.
- W2PP comparison: `Server/W2PP/Source/Code/Basedef.cpp::BASE_GetCurrentScore`, `Type == 16`, source SHA-256 `C9E04E7F0AAC87DFDAB367B77F3DC8384355FE1166898FFEB62E095C551B99F7`. Base rows and sequential arithmetic match. Its `AcAdd` differs for skilled Wolf (3 rather than 0), Titan (10 rather than 20), and Eden (5 rather than 10); Astaroth's skilled +5 matches. W2PP also adds +5 AC per valid Wolf.
- Port: `LegacyCurrentScoreMath.CalculateTransformationArmorClassAffectStage` returns `HasActiveTransformation` plus the sequentially transformed AC. It does not mutate MOB/equipment, recalculate MaxHP or other transform outputs, apply unrelated affects, or integrate with character login.
- Fixture: `character-login-transformation-ac.golden.json` contains 19 synthetic vectors for every form, skill/class/value gates, source-order truncation, repeated and reordered transforms, negative AC division, and the maximum unsigned-short affect level. The independent test-only W2PP evaluator checks the explicit comparator outputs. These are source-derived vectors, not compiled C++ runtime captures.
- Scope: Type=16 AC only. Type=16 MaxHP, regeneration, critical/resistance/equipment mutation, score-stage integration, and client E2E remain separate or unfinished.
