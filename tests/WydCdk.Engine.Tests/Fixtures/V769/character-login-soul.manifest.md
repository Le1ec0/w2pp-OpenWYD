# Character-login Soul calculation vectors

- Target: `Backup/Tools/ReferenceSources/TMProject2GlobalClient/Servidor/Source/Code/Basedef.cpp::BASE_GetCurrentScore` (Soul block at line 3215); SHA-256 `D84B2E358825CC0AF1FF8D0CFD316E09304892C15422A48C3D94A3D047927610`.
- Header: `Servidor/Source/Code/Basedef.h`; SHA-256 `B59A7C5FC76D898C10E3600005920A56450725480C8C5D485DE8F199762307E8`.
- Call site: `Servidor/Source/Code/TMSrv/CMob.cpp::GetCurrentScore` (line 619); login invokes `BASE_GetHpMp` and then `GetCurrentScore`.
- Build selection: `TMSrv/TMSrv.vcxproj:101` (SHA-256 `981FB39A978FFB90A07E6AA2315C0E01639491C1B428AA96BC8E212E64176802`) compiles `..\\Basedef.cpp`, not the adjacent `TMSrv/Basedef.cpp`; this selects the common helper, while the already documented 16/18 structure mismatch prevents treating the source tree as a closed runtime oracle.
- Affect contract: `Basedef.h` defines `MAX_AFFECT=32` and an eight-byte `STRUCT_AFFECT` whose `Type` is byte 0; Soul is active when `Type == 29`. `STRUCT_MOBExtra` supplies `ClassMaster` and `Soul`.
- W2PP comparison: `Server/W2PP/Source/Code/Basedef.cpp::BASE_GetCurrentScore` (line 3014; SHA-256 `C9E04E7F0AAC87DFDAB367B77F3DC8384355FE1166898FFEB62E095C551B99F7`) has a same-named Soul block, but it only derives temporary `bInt`/`bCon` values. It then applies equipment attribute deltas (`ItemInt`/`ItemCon`) to MaxHP/MaxMP; it does not apply the Soul-derived values. 7.69 instead adds the selected Soul's scaled Int/Con contribution per active Type-29 affect. This is treated as an intentional target divergence, not copied from W2PP.
- Multipliers: Mortal uses 1.8/1.6/1.4 by Soul code; non-Mortal/non-Arch uses 2.2/1.8/1.4. Arch gets no Soul adjustment. Scaling uses the source's single-precision multiplication and truncation to integer, then adds twice the scaled stat to MaxMP (Int) or MaxHP (Con). Multiple active Type-29 records repeat the adjustment, mutating the temporary stat between records, as in source.
- Fixture: `character-login-soul.golden.json`; 31 source-derived arithmetic cases cover all HP/MP-affecting ordinary Soul codes for Mortal and non-Mortal/non-Arch masters, Celestial/CS/S-Celestial masters, fractional truncation, absent/other affects, repeated Soul affects, and summon bypass. Mortal vectors use level 400 to select ordinary Mortal Soul rather than the low-level Kibita branch. Values are synthetic; not captured player data or a compiled C++ runtime oracle.
- Scope: this early stage only changes `CurrentScore.MaxHp` and `CurrentScore.MaxMp`; current HP/MP, attributes, BaseScore, and all other fields are preserved. For Mortal level <369 it now skips ordinary Mortal Soul scaling; the later Kibita override is separately ported in `character-login-kibita-soul.manifest.md`. Resistances, clan, equipment mutation, and remaining derived stats are not part of this method. It remains isolated from the CharacterLogin adapter/listener. The 7.69 source build still has the documented TMSrv/common structure-width inconsistency, so these vectors prove the selected source-level calculation, not runtime ABI closure.
