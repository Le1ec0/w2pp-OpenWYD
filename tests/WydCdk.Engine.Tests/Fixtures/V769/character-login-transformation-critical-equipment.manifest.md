# Character-login transformation Critical and equipment stages (7.69)

- Target: `Backup/Tools/ReferenceSources/TMProject2GlobalClient/Servidor/Source/Code/Basedef.cpp::BASE_GetCurrentScore`, `Affect.Type == 16` at lines 3711-3822. The selected common file is included by `TMSrv.vcxproj` as `..\\Basedef.cpp` (project line 101). Source SHA-256: `D84B2E358825CC0AF1FF8D0CFD316E09304892C15422A48C3D94A3D047927610`.
- Critical target behavior: a valid class-2 transformation with Wolf skill `0x20000` adds 5 Critical; Eden adds 10 unconditionally; Bear, Astaroth, and Titan add none. Contributions accumulate for repeated valid Type=16 affects. The existing Critical seed and final 0..255 clamp are separate stages.
- Equipment target behavior: every valid value 1..5 for class 2 replaces `Equip[0].sIndex` with 22, 23, 24, 25, or 32 (Eden). It then overwrites effect 0 with `EF_SANC`/`LegacyItemEffect.Sanctuary` and the calculated value; effects 1/2 are preserved. The previous `Traje` index is read but not restored by the compiled branch. The last valid affect therefore determines the returned head item.
- Sanctuary formula: `sanc = (special3 + levelTerm*2)/3`, then `(sanc - pTransBonus[value].Unk17)/12`, clamped to 0..9. `levelTerm` is the character `CurrentScore.Level` for ARCH/MORTAL and `Level + MAX_LEVEL` otherwise; `Affect.Level` is not used. The target base-sanc row is 15 for Wolf and 100 for the other forms. `special3` is the already capped prior-stage value.
- W2PP comparison: `Server/W2PP/Source/Code/Basedef.cpp::BASE_GetCurrentScore`, Type=16 branch, source SHA-256 `C9E04E7F0AAC87DFDAB367B77F3DC8384355FE1166898FFEB62E095C551B99F7`. Equipment index, sanctuary formula, effect mutation, and preservation behavior match. Critical diverges for skilled Astaroth (W2PP +5; target 0) and Eden (W2PP +6; target +10); Wolf +5 matches.
- Port: `CalculateTransformationCriticalAffectStage` returns only the accumulated Critical addition. `CalculateTransformationEquipmentAffectStage` returns a new `LegacyItem` and never mutates the input item or MOB. Neither stage applies the existing Critical seed/final clamp, recalculates other transformation stats, or integrates with character login.
- Fixture: `character-login-transformation-critical-equipment.golden.json` contains 20 synthetic vectors covering all forms, critical divergences, item indices/effect replacement, sanctuary formula/class-master branches, repeated/invalid affects, class/value gates, and the distinction between character level and Affect.Level. These are source-derived vectors, not compiled C++ runtime captures.
- Scope: Type=16 Critical and head-item mutation only. Final score composition, persistence policy for transformed equipment, and client E2E remain separate or unfinished.
