# Character-login transformation MaxHp stage (7.69)

- Target: `Backup/Tools/ReferenceSources/TMProject2GlobalClient/Servidor/Source/Code/Basedef.cpp::BASE_GetCurrentScore`, `Affect.Type == 16` at lines 3711-3817, with the MaxHp multiplication at 3806-3812. The selected common file is included by `TMSrv.vcxproj` as `..\\Basedef.cpp` (project line 101). Source SHA-256: `D84B2E358825CC0AF1FF8D0CFD316E09304892C15422A48C3D94A3D047927610`.
- Formula per valid value 1..5 and class 2: `min = HpAdd + pTransBonus[value-1].Unk5`, `max = HpAdd + Unk6`, `multi = (max-min)*Affect.Level/200 + min`; then mutate the current `MaxHp` as `MaxHp = MaxHp*multi/100` with source integer truncation. Multiple valid affects are applied sequentially in slot order. No clamp occurs inside this branch.
- Target `HpAdd`: Wolf 0 even with skill; Bear 10 only with skill `0x80000`; Astaroth 5 only with `0x200000`; Titan 5; Eden 10. Base Min/Max HP rows are `[95,105]`, `[110,140]`, `[100,120]`, `[105,110]`, `[105,115]`.
- W2PP comparison: `Server/W2PP/Source/Code/Basedef.cpp::BASE_GetCurrentScore`, Type=16 branch, source SHA-256 `C9E04E7F0AAC87DFDAB367B77F3DC8384355FE1166898FFEB62E095C551B99F7`. The five base HP rows and sequential arithmetic match. `HpAdd` differs for skilled Wolf (5 rather than 0) and skilled Bear (15 rather than 10); Astaroth/Titan/Eden match. Independent W2PP values are checked by the test-only comparator.
- The legacy local name `RegAdd` is misleading for this mutation: the branch adds that amount to Sagrado/Fogo/Trovao/Gelo, i.e. the four resistance accumulators. This is already covered by `ApplyResistanceAffectStage` and its 14-vector `character-login-resistance-affects` fixture; this MaxHp recut intentionally does not duplicate it.
- Port: `LegacyCurrentScoreMath.CalculateTransformationMaxHpAffectStage` returns `HasActiveTransformation` and the sequentially updated MaxHp. It does not mutate MOB, apply final MaxHp clamps, include unrelated affects, or integrate into character login.
- Fixture: `character-login-transformation-maxhp.golden.json` contains 20 synthetic vectors for all forms, skill/class/value gates, order and integer truncation, repeated transforms, and maximum unsigned-short affect level. These are source-derived vectors, not compiled C++ runtime captures.
- Scope: Type=16 MaxHp only. Critical and equipment mutations, final score composition, and client E2E remain separate or unfinished.
