# Character-login resistance affects (7.69)

- Target: `Backup/Tools/ReferenceSources/TMProject2GlobalClient/Servidor/Source/Code/Basedef.cpp::BASE_GetCurrentScore`, resistance seed at lines 3207-3210, affect mutations at lines 3600-3607 (`Type=3`), 3691-3709 (`Type=25`), 3711-3817 (`Type=16`), and 3992-4001 (`Type=8`). Source SHA-256: `D84B2E358825CC0AF1FF8D0CFD316E09304892C15422A48C3D94A3D047927610`.
- Affect ABI: `Basedef.h::STRUCT_AFFECT` is eight bytes (`Type`, `Value`, `Level`, `Time`) and `MAX_AFFECT=32`; header SHA-256: `B59A7C5FC76D898C10E3600005920A56450725480C8C5D485DE8F199762307E8`.
- Target behavior: `Type=3` subtracts `Value/2` when the current face index is below 50, otherwise `Value-10`, from all four raw resistances. `Type=25` adds `(Value + Level/4)/10`, plus 20 at `Level>=255`, to all four including holy. `Type=16` for `MOB.Class=2` updates the face index in affect order and adds transformation resistance: wolf 5 with skill `0x20000`, bear 20 with `0x80000`, Astaroth 15 with `0x200000`, Titan 20, Eden 20. The resistance bit in `Type=8` (`Level & 2`) adds 25 to each resistance in the source's later affect pass.
- W2PP comparison: `Server/W2PP/Source/Code/Basedef.cpp::BASE_GetCurrentScore`, function SHA-256 `C9E04E7F0AAC87DFDAB367B77F3DC8384355FE1166898FFEB62E095C551B99F7`. `Type=3` and `Type=8` agree. W2PP `Type=25` omits holy. Transformation additions differ: wolf 0, bear 40, Astaroth 15, Titan 0, Eden 10. Tests independently evaluate those W2PP formulas against the fixture's comparison vectors.
- Exclusion: the target's `ImuItens` equipment loop that would sum `EF_RESISTALL` is commented out. This stage deliberately does not activate equipment resistance effects. It also does not port the other stat, damage, defense, HP, critical, or regeneration side effects of `Type=16`/other buffs; its Attack/Run contribution has a separate isolated recut in `character-login-transformation-speed.manifest.md`.
- Port: `LegacyCurrentScoreMath.ApplyResistanceAffectStage` returns raw resistance totals before `ApplyResistanceClamps` and the resulting head index. Keeping the face index in the returned snapshot preserves the later `Type=3` branch when a prior `Type=16` has transformed the face. The affect snapshot is read-only and must contain all 32 eight-byte entries.
- Fixture: `character-login-resistance-affects.golden.json` has 14 synthetic vectors covering both `Type=3` branches, `Type=25` threshold/integer division, `Type=8`, every transformation, skill/class gates, and both transform/pursuit orders. It is source-derived arithmetic evidence, not player data or compiled C++ runtime capture.
- Scope: pure isolated resistance calculation only. No MOB mutation, transformation pipeline integration, Affect wire, listener integration, or client E2E is included; apply final resistance clamps as the following stage.
